manager={{manager}}
workers={{workers}}
user=$USER # set this to the username for the VMs (GENI or similar) if different from local username
registry_user=nieda2018 # set this to hub.docker username
tcep_image=tcep
gui_image=tcep-gui
output_dir=~/Downloads
n_speed_streams=8 # if using non-default query: set to max. number of stream operators with speed publishers used in the query (SimulationSetup.scala:runSimulation() )
n_density_streams=1 # same as speed streams but for density publishers
n_publisher_nodes_total=0 # adjusted by script
